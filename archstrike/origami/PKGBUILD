# Generated by gem2arch (https://github.com/anatol/gem2arch)
# Maintainer: ArchStrike <team@archstrike.org>

buildarch=1

pkgname=origami
pkgver=2.0.1
pkgrel=1
pkgdesc='Aims at providing a scripting tool to generate and analyze malicious PDF files.'
groups=('archstrike' 'archstrike-malware')
arch=('any')
url='http://code.google.com/p/origami-pdf'
license=("LGPL3")
depends=("ruby")
options=("!emptydirs")
install="${pkgname}.install"
source=("https://rubygems.org/downloads/${pkgname}-${pkgver}.gem")
noextract=("${pkgname}-${pkgver}.gem")
depends=('ruby' 'ruby-gtk2' 'ruby-colorize')
sha512sums=('2376274feced430d78de0d7a2789bf8c645200c51a4af03316a23963873b4600191c6f04e946281360e9ea7fd06d5d2713229dc1d8b36ee746c650c94ab8adca')

package() {
  local _gemdir="$(ruby -e'puts Gem.default_dir')"
  gem install --ignore-dependencies --no-user-install -i "${pkgdir}/${_gemdir}" -n "${pkgdir}/usr/bin" ${pkgname}-${pkgver}.gem
  rm "${pkgdir}/${_gemdir}/cache/${pkgname}-${pkgver}.gem"
  install -D -m644 "${pkgdir}/${_gemdir}/gems/${pkgname}-${pkgver}/COPYING.LESSER" "${pkgdir}/usr/share/licenses/${pkgname}/COPYING.LESSER"
}
