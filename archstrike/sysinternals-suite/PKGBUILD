# Maintainer: ArchStrike <team@archstrike.org>

buildarch=1

pkgname=sysinternals-suite
pkgver=20210601
pkgrel=1
groups=('archstrike')
pkgdesc="Sysinternals tools suite"
url="http://sysinternals.com"
license=('sysinternals')
arch=('any')
makedepends=('unzip')
source=(http://download.sysinternals.com/files/SysinternalsSuite.zip)
sha512sums=('6f1b56e539c7fb5faa97cefa12e1a300e9d01d0d1361411a22df7520e0776774d9b31806199db2a1e3d283b7cafe324318f124e05d58a444fc31136b456e2094')

package() {
  cd "$srcdir"

  # Base directory.
  install -dm755 "$pkgdir/usr/share/sysinternals-suite"

  find . -type l -exec rm {} \;

  # Transform all files to lowercase
  for f in *; do
    lc=$(echo $f | tr '[A-Z]' '[a-z]')

    if [ ! -f $f -o -f $lc ]; then
      continue
    fi

    mv -i $f $lc
  done

  install -m755 *.exe *.sys "$pkgdir/usr/share/sysinternals-suite"
  install -m644 *.chm *.hlp *.txt "$pkgdir/usr/share/sysinternals-suite"
}
