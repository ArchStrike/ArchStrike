# Maintainer: ArchStrike <team@archstrike.org>

buildarch=212

pkgname=codecrypt
pkgver=1.7.3
pkgrel=1
pkgdesc="A GnuPG-like program for encryption and signing that uses only quantum-computer-resistant algorithms"
url="https://github.com/exaexa/$pkgname"
license=('GPL3')
groups=('archstrike-crypto')
arch=('i686' 'x86_64' 'armv6h' 'armv7h')
depends=('crypto++' 'gmp' 'fftw')
makedepends=('libtool' 'automake' 'autoconf')
source=("https://github.com/exaexa/codecrypt/archive/v$pkgver.tar.gz")
sha512sums=('1aedd9fd00d1890f7cbc52d18e8324e882a75a40efb17426b2eda2526ba7aea6b05e3621738c76e5c8bd743cf485efcb8a67e34021a7decab80534763136c8f3')

build() {
    LDFLAGS="$LDFLAGS -pthread"

    cd "$pkgname-$pkgver"
    ./autogen.sh
    ./configure --prefix=/usr
    make
}

package() {
    cd "$pkgname-$pkgver"
    make DESTDIR="$pkgdir" install
}
