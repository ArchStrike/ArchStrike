# Generated by gem2arch (https://github.com/anatol/gem2arch)
# Maintainer

_gemname=meterpreter_bins
pkgname=ruby1.9-$_gemname
pkgver=0.0.6
pkgrel=1
pkgdesc='This gem contains the compiled binaries required to make Meterpreter function for Windows 32-bit and 64-bit targets. Other binaries will be added in future versions.'
arch=(any)
url='http://www.metasploit.com'
license=('3-clause (or "modified") BSD')
depends=(ruby1.9)
options=(!emptydirs)
source=(https://rubygems.org/downloads/$_gemname-$pkgver.gem)
noextract=($_gemname-$pkgver.gem)
sha512sums=('d9c51bd61643c41d882db9f6a92477af46cbad6f78428861f52f1bcb283ab1c24a41b297cc70993c7573b0e7578631635beaa83c2bad2170ded7064c5a98c739')

package() {
  local _gemdir="$(ruby-1.9 -e'puts Gem.default_dir')"
  gem-1.9 install --ignore-dependencies --no-user-install -i "$pkgdir/$_gemdir" -n "$pkgdir/usr/bin" $_gemname-$pkgver.gem
  rm "$pkgdir/$_gemdir/cache/$_gemname-$pkgver.gem"
  install -D -m644 "$pkgdir/$_gemdir/gems/$_gemname-$pkgver/LICENSE" "$pkgdir/usr/share/licenses/$pkgname/LICENSE"
  install -D -m644 "$pkgdir/$_gemdir/gems/$_gemname-$pkgver/LICENSE.txt" "$pkgdir/usr/share/licenses/$pkgname/LICENSE.txt"
}
