# Maintainer: ArchStrike <team@archstrike.org>

buildarch=1

pkgname=archstrike-xfce-config
pkgver=1.0
pkgrel=1
groups=('archstrike-desktop')
arch=('any')
pkgdesc="ArchStrike config files for Xfce"
url="https://github.com/ArchStrike/archstrike-xfce-config"
license=('GPL3')
depends=('xfce4' 'xfce4-goodies' 'network-manager-applet' 'numix-themes' 'ttf-hack')
source=("https://github.com/ArchStrike/archstrike-xfce-config/archive/${pkgver}.tar.gz")
sha512sums=('311fa5ea8f4d0a31c375f5db0fbc8015c0f0f2be0daf11966d9eb967cef80f0527cb5b12a701c8f664d947903a7b1eb615b675b1c57ddcb4250efd0a621773e2')

package() {
  cd ${pkgname}-${pkgver}
  install -dm755 "${pkgdir}/usr/share/${pkgname}"

  cp -a --no-preserve=ownership * "${pkgdir}/usr/share/${pkgname}"

}

